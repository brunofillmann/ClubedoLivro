<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="clubedolivro.clubelivro">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!-- <activity -->
        <!-- android:name=".ScrollingActivity" -->
        <!-- android:label="@string/title_activity_scrolling" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!-- <activity android:name=".Frames" /> -->
        <activity
            android:name=".Perfil2"
            android:label="@string/title_activity_perfil2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Feed"
            android:label="@string/title_activity_feed">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".Cadastro1"
            android:label="@string/title_activity_cadastro1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Estante"></activity>
    </application>

</manifest>